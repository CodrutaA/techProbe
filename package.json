{
  "name": "techprobe",
  "version": "1.0.0",
  "description": "POC for testing e-comm app",
  "main": "app.js",
  "scripts": {
    "start": "nodemon --watch src src/app.js",
    "wait:for:server": "wait-on tcp::1111 -t 10s",
    "test": "playwright test",
    "report": "npx playwright show-report",
    "playwright:install:browsers": "playwright install --with-deps"
  },
  "keywords": [
    "e-commerce",
    "poc",
    "testing"
  ],
  "author": "Codruta Avram",
  "license": "ISC",
  "engines": {
    "node": ">18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.3",
    "jest": "^29.3.1",
    "nodemon": "^3.0.1",
    "wait-on": "^7.2.0"
  }
}
